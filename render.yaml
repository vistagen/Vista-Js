# Render Blueprint - Vista Web Application
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: vista-web
    runtime: node
    region: oregon
    plan: free
    buildCommand: |
      cd packages/vista && NODE_ENV=development npm install --ignore-scripts --no-audit --no-fund && cd ../../apps/web && NODE_ENV=development npm install --no-audit --no-fund && npx tsx ../../packages/vista/bin/vista.js build
    startCommand: cd apps/web && npx tsx ../../packages/vista/bin/vista.js start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3003
    healthCheckPath: /
