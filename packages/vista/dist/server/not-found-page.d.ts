/**
 * Styled 404 fallback page — used when no custom not-found component exists.
 * Vista's own design: dark background with a subtle animated gradient accent.
 */
export declare function getStyledNotFoundHTML(): string;
